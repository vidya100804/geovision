{
  "name": "geovision-root",
  "private": true,
  "scripts": {
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build && cd .. && rm -rf dist && cp -r frontend/dist dist"
  }
}
